# POS-94-3 — QA release APK (Bluetooth print verification)

## Build artifact

| Field | Value |
|-------|-------|
| APK path | `/workspace/build/app/outputs/flutter-apk/app-release.apk` |
| APK size | 65,936,072 bytes (~62.9 MiB) |
| Git commit SHA | `36cab62376c8a53163e3863949fe3486502f5c66` |
| Build type | `release` |
| `API_BASE_URL` | `https://pos-api.cymonevo.com` |
| `APP_ENV` | `staging` |

## QA `.env` (local only, git-ignored)

```
API_BASE_URL=https://pos-api.cymonevo.com
SENTRY_DSN=
APP_ENV=staging
```

## Pre-verification

| Test case | Result | Notes |
|-----------|--------|-------|
| Release APK build succeeds | PASS | `scripts/build-apk.sh release` exited 0; last stdout line is `/workspace/build/app/outputs/flutter-apk/app-release.apk`; file exists and is > 1 MB. |
| CI regression before device handoff | PASS | `make ci` on commit `36cab62` — analyzer clean, 223 tests passed. |
| APK launches against QA API | PASS | `adb install -r` succeeded on emulator; app launched to login screen ("Welcome Back", email/password fields, Login button) without Flutter crash. QA `GET /healthz` returns `{"success":true,"data":{"status":"ok"}}`. Bundled `.env` in APK confirmed `API_BASE_URL=https://pos-api.cymonevo.com`. |

## Distribution

Nextcloud upload skipped — `rclone` not available in this environment.
