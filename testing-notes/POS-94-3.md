# POS-94-3 — QA release APK (Bluetooth print verification)

## Build artifact

| Field | Value |
|-------|-------|
| APK path | `/workspace/build/app/outputs/flutter-apk/app-release.apk` |
| APK size | 66,034,288 bytes (~63.0 MiB) |
| Git commit SHA | `f8afccfeb04119bf862c372eb35ecb9265dcb88f` |
| Build type | `release` |
| `API_BASE_URL` | `https://pos-api.cymonevo.com` |
| `APP_ENV` | `staging` |
| Verified at | 2026-07-18 UTC |

## QA `.env` (local only, git-ignored)

```
API_BASE_URL=https://pos-api.cymonevo.com
SENTRY_DSN=
APP_ENV=staging
```

## Pre-verification

| Test case | Result | Notes |
|-----------|--------|-------|
| Release APK build succeeds | PASS | `scripts/build-apk.sh release` exited 0; last stdout line is `/workspace/build/app/outputs/flutter-apk/app-release.apk`; file exists (66,034,288 bytes, > 1 MB). |
| Bundled env targets staging QA API | PASS | `unzip -p app-release.apk assets/flutter_assets/.env` contains `API_BASE_URL=https://pos-api.cymonevo.com` and `APP_ENV=staging`. |
| QA API health check succeeds | PASS | `curl -sS https://pos-api.cymonevo.com/healthz` → `{"success":true,"data":{"status":"ok"}}`. |
| QA cashier login succeeds | PASS | `POST /api/v1/auth/login` with `cashier-test@cymonevo.com` returned `success: true` and non-empty `data.tokens.access_token`. |
| APK installs on emulator | FAIL | `scripts/start-shared-emulator.sh` (Luna_Test_Lite, software rendering fallback) failed: x86_64 emulator requires KVM (`/dev/kvm` not available). Emulator process exited before any adb serial appeared. |
| APK launches to login screen without crash | FAIL | Blocked by emulator failure; no successful `adb install -r`. |
| Testing notes updated with build metadata | PASS | This file updated on commit `f8afccfeb04119bf862c372eb35ecb9265dcb88f`. |

## CI

`make ci` on commit `f8afccfeb04119bf862c372eb35ecb9265dcb88f` — analyzer clean, all unit/widget tests passed.

## Emulator / native launch

| Field | Value |
|-------|-------|
| Emulator serial | *(none — native verification blocked)* |
| AVD attempted | `Luna_Test_Lite` |
| Block reason | KVM unavailable (`/dev/kvm` not found). Android x86_64 emulator requires hardware acceleration; `-gpu swiftshader_indirect` does not satisfy CPU acceleration requirement. |
| Emulator log excerpt | `ERROR | x86_64 emulation currently requires hardware acceleration! CPU acceleration status: /dev/kvm is not found` |

## Web fallback smoke test

Native launch could not be verified on emulator. Fallback per POS-98-2 instructions:

- `flutter build web --release` exited 0.
- Bundled `build/web/assets/.env` matches staging QA config.
- `build/web/main.dart.js` contains login heading string `Welcome Back`.
- Widget test `unauthenticated startup routes to the login screen` passed (`LoginPage` renders without error).

## Distribution

Nextcloud upload skipped — `rclone` not available in this environment.
