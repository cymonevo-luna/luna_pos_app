/// Record-management feature keys returned by the auth and user profile APIs.
///
/// Keys must match the backend privilege registry exactly.
abstract final class RecordsFeatures {
  static const editDate = 'records.edit_date';
}
